# NEON RED - Vue 3 версия

## 📋 Описание

Этот проект представляет собой переписанную версию интернет-магазина NEON RED с использованием Vue 3 и Composition API. Все прямые манипуляции DOM заменены на реактивные данные Vue, код разделён на логические компоненты и композаблы.

## 🚀 Структура проекта

```
neon-red-main/
├── src/
│   ├── components/          # Vue компоненты
│   │   ├── AgeVerification.vue
│   │   ├── AuthModal.vue
│   │   ├── CartModal.vue
│   │   ├── Header.vue
│   │   ├── LoginForm.vue
│   │   ├── Modal.vue
│   │   ├── ProductCard.vue
│   │   ├── ProductList.vue
│   │   ├── ProductPagination.vue
│   │   ├── ProfileField.vue
│   │   ├── ProfileModal.vue
│   │   ├── RegisterForm.vue
│   │   ├── StepIndicator.vue
│   │   └── ToastContainer.vue
│   ├── composables/         # Композаблы (переиспользуемая логика)
│   │   ├── useApi.js        # Работа с API
│   │   ├── useAuth.js       # Аутентификация
│   │   ├── useCart.js       # Корзина
│   │   ├── useProducts.js   # Товары
│   │   └── useToast.js      # Уведомления
│   ├── styles/
│   │   └── main.css         # Глобальные стили
│   ├── App.vue              # Главный компонент
│   └── main.js              # Точка входа
├── index.html                # HTML шаблон
├── vite.config.js           # Конфигурация Vite
└── package.json             # Зависимости
```

## ✨ Ключевые улучшения

### 1. Архитектура

- **Разделение на компоненты**: Каждая функциональная область вынесена в отдельный компонент
- **Композаблы**: Переиспользуемая логика вынесена в композаблы (`useAuth`, `useCart`, `useProducts`, `useApi`, `useToast`)
- **Реактивность**: Все данные реактивны через `ref` и `reactive`
- **Отсутствие прямых манипуляций DOM**: Вместо `document.querySelector` используется реактивная система Vue

### 2. Читаемость кода

- **Composition API**: Современный подход с `setup` и композаблами
- **Типизация**: Использование TypeScript-подобных проверок через props
- **Именование**: Понятные имена компонентов и функций
- **Структура**: Логическая группировка файлов

### 3. UX/UI улучшения

- **Анимации**: Плавные переходы для модальных окон и списков
- **Загрузка**: Skeleton-экраны вместо пустых состояний
- **Обратная связь**: Toast-уведомления для всех действий
- **Адаптивность**: Сохранена полная адаптивность оригинального дизайна

## 🔧 Технологии

- **Vue 3.4+** - Фреймворк
- **Composition API** - Современный API Vue
- **Vite** - Сборщик и dev-сервер
- **CSS Scoped** - Изолированные стили компонентов

## 📦 Установка и запуск

### Предварительные требования

1. **Запустите backend сервер** (обязательно):
```bash
# В корне проекта запустите backend
npm install  # если еще не установлены зависимости
npm run server    # запуск backend сервера
# или для разработки с автоперезагрузкой:
npm run server:dev
# Backend будет доступен на http://localhost:3001
```

2. **Настройте переменные окружения** (опционально):
```bash
# Создайте .env файл в корне проекта
cp .env.example .env
# Отредактируйте .env и укажите VITE_API_URL если нужно
```

### Запуск Vue приложения

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
# Приложение будет доступно на http://localhost:3000

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

### ⚠️ Важно

**Backend сервер должен быть запущен** перед запуском Vue приложения, иначе вы получите ошибки подключения (`ERR_CONNECTION_REFUSED`).

Если backend запущен на другом порту или URL, укажите это в `.env` файле:
```
VITE_API_URL=http://your-backend-url/api/v1
```

## 🔑 Основные компоненты

### Композаблы

#### `useApi`
- Универсальная работа с API
- Автоматическая обработка ошибок
- Retry логика для сетевых ошибок
- Индикатор загрузки

#### `useAuth`
- Управление аутентификацией
- Сохранение токена и пользователя
- Валидация токена
- Методы `login`, `logout`, `validateToken`

#### `useCart`
- Управление корзиной
- Добавление/удаление товаров
- Изменение количества
- Оформление заказа

#### `useProducts`
- Загрузка товаров
- Пагинация
- Управление состоянием загрузки

#### `useToast`
- Система уведомлений
- Автоматическое скрытие
- Разные типы (success, error, info)

### Компоненты

#### `App.vue`
Главный компонент приложения, управляет состоянием модальных окон и координацией между компонентами.

#### `Header.vue`
Шапка сайта с кнопками навигации, корзиной и профилем.

#### `ProductList.vue`
Список товаров с поддержкой пагинации и состояний загрузки.

#### `AuthModal.vue`
Модальное окно авторизации с переключением между входом и регистрацией.

#### `CartModal.vue`
Модальное окно корзины с управлением товарами и оформлением заказа.

#### `ProfileModal.vue`
Модальное окно профиля пользователя с информацией и заказами.

## 🎯 Отличия от оригинальной версии

### Убрано:
- ❌ Прямые манипуляции DOM (`document.querySelector`, `innerHTML`, `addEventListener`)
- ❌ Глобальные переменные
- ❌ Классы для модулей (заменены на композаблы)
- ❌ Ручное управление состоянием через DOM

### Добавлено:
- ✅ Реактивные данные (`ref`, `reactive`, `computed`)
- ✅ События Vue (`@click`, `@input`, `@submit`)
- ✅ Двусторонняя привязка (`v-model`)
- ✅ Композаблы для переиспользования логики
- ✅ Компонентная архитектура
- ✅ Scoped стили для изоляции CSS

## 🐛 Исправленные проблемы

1. **Утечки памяти**: Правильная очистка таймеров и подписок
2. **Обработка ошибок**: Централизованная обработка через `useApi`
3. **Состояние загрузки**: Единый индикатор загрузки
4. **Валидация**: Реактивная валидация форм
5. **Доступность**: Улучшена поддержка ARIA-атрибутов

## 📝 Примечания

- Для работы требуется запущенный backend сервер (см. `server.cjs`)
- API URL настраивается через переменную окружения `VITE_API_URL` (по умолчанию `http://localhost:3001/api/v1`)
- Все стили перенесены в компоненты с `scoped` атрибутом
- Сохранена полная функциональность оригинального приложения

## 🔄 Миграция с оригинальной версии

Основные изменения для разработчиков:

1. **Импорты**: Вместо классов импортируются композаблы
   ```js
   // Было
   import { AuthModule } from './auth.js'
   
   // Стало
   import { useAuth } from './composables/useAuth.js'
   ```

2. **Реактивность**: Данные теперь реактивные
   ```js
   // Было
   this.user = data.user
   
   // Стало
   user.value = data.user
   ```

3. **События**: Используются директивы Vue
   ```html
   <!-- Было -->
   <button onclick="handleClick()">
   
   <!-- Стало -->
   <button @click="handleClick">
   ```

## 📄 Лицензия

Проект создан для демонстрации миграции на Vue 3.

